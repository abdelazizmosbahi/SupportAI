# ===== Application =====
APP_NAME=SupportAI
APP_VERSION=0.1.0
# development | production
APP_ENV=development
DEBUG=true

# ===== Database =====
# Copy this file to backend/.env and put real secrets there (backend/.env is gitignored).
DATABASE_URL=postgresql+asyncpg://supportai:supportai@localhost:5432/supportai
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=10

# ===== Redis =====
REDIS_URL=redis://localhost:6379/0

# ===== MinIO =====
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_DOCUMENTS=documents
MINIO_BUCKET_AVATARS=avatars
MINIO_BUCKET_EXPORTS=exports
MINIO_SECURE=false

# ===== JWT =====
# NEVER hardcode a real secret in source code. Set it in backend/.env.
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ===== LLM =====
LLM_PROVIDER=ollama
LLM_MODEL=qwen2:0.5b
LLM_HOST=http://localhost:11434
LLM_TIMEOUT_SECONDS=60

# ===== Embeddings =====
EMBEDDING_MODEL=all-MiniLM-L6-v2
EMBEDDING_DIMENSIONS=384

# ===== RAG =====
RAG_TOP_K=5
RAG_CHUNK_SIZE=800
RAG_CHUNK_OVERLAP=100

# ===== File Upload =====
MAX_UPLOAD_SIZE_MB=10
ALLOWED_EXTENSIONS=["pdf", "txt", "docx"]

# ===== Rate Limiting =====
RATE_LIMIT_LOGIN_PER_MINUTE=5
RATE_LIMIT_API_PER_MINUTE=100
RATE_LIMIT_AI_PER_MINUTE=20

# ===== CORS =====
CORS_ORIGINS=["http://localhost:5173"]

# ===== Logging =====
LOG_LEVEL=INFO
